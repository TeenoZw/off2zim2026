# Main Web App

Customer-facing web surface for:

- landing page
- explorer login and registration
- discovery, planning, and booking flows

Run locally:

```bash
npm run dev
```

This starts the shared Next.js codebase in `public` surface mode on port `3000`.
