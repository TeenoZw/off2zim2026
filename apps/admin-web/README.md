# Admin Web App

Administrator workspace for:

- admin sign in
- provider moderation
- booking oversight
- dispute handling

Run locally:

```bash
npm run dev
```

This starts the shared Next.js codebase in `admin` surface mode on port `3003`.
