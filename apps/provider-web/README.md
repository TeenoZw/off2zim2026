# Provider Web App

Service provider workspace for:

- provider login and registration
- listings
- orders
- verification

Run locally:

```bash
npm run dev
```

This starts the shared Next.js codebase in `provider` surface mode on port `3002`.
